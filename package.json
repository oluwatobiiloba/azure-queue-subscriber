{
  "name": "azure-queue-consumer",
  "version": "0.0.1",
  "description": "Port of sqs-consumer for Azure data queue",
  "main": "index.js",
  "scripts": {
    "test": "mocha",
    "coverage": "istanbul cover _mocha -- -R dot && open coverage/lcov-report/index.html",
    "lcov": "istanbul cover _mocha -- -R dot",
    "lint": "eslint .",
    "posttest": "npm run lint"
  },
  "devDependencies": {
    "codeclimate-test-reporter": "0.4.1",
    "eslint": "^2.9.0",
    "eslint-config-iplayer-es6": "^1.2.0",
    "istanbul": "^0.4.1",
    "jest": "^29.5.0",
    "mocha": "^2.5.3",
    "sinon": "^4.4.8"
  },
  "dependencies": {
    "@azure/storage-queue": "^12.13.0",
    "async": "^2.0.1",
    "auto-bind": "^1.1.0",
    "azure-storage": "^2.8.2",
    "debug": "^2.1.0",
    "dotenv": "^16.0.3"
  },
  "eslintConfig": {
    "extends": "iplayer-es6",
    "parserOptions": {
      "ecmaVersion": 2017
    }
  }
}
